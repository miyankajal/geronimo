<h1>Listing tags</h1>

</br>
<% if current_user.type == 2 %>
	<div class="span7" style="width:100%;">
		<div class="row">
			<div class="well">
				<div class="alert alert-dismissable alert-success">
					Students will only be allowed to add tags that have 0 points associated with them. The tags with higher points can only be assigned by the teachers.
				</div>
			</div>
		</div>
	</div>

	<%= render 'form' %>
<% end %>

<table class="table table-striped table-hover" id='tags_table'>
  <thead>
    <tr>
      <th>Description</th>
      <th>Point</th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @tags.each do |tag| %>
      <%= render 'tag', tag: tag %>
    <% end %>
  </tbody>
</table>

