<h1>Listing <%= params[:desc] %> </h1>

</br>
<%= link_to 'New User', new_user_path(:type => params[:type]) %>
<hr>

<table class="table table-striped table-hover">
  <thead>
    <tr>
      <% if params[:type].to_s == "3" %>
      	<th>Enrollment ID</th>
      <% end %>
      <th>Username</th>
      <th>Name</th>
      <th>Action</th>
    </tr>
  </thead>
  <tbody>
	<% @users.each do |user| %>
		<%= render user, type: params[:type], guardian_id: params[:guardian_id], search: params[:search] %>
	<% end %>
  </tbody>
</table>

<br>

<h3>Import Users</h3>

<%= link_to "Sample Excel File", :controller => 'users', :action => 'download_sample', :id => current_user.id %>
</br></br>

<div class="span7">
	<div class="row">
		<div class="well" style="width:100%;">
			<div class="alert alert-dismissable alert-success">
				To add users with the help of an Excel sheet please download the Sample Excel File. Remove the sample users and replace with your users. Fill in the appropriate Type: 1 - Administrators, 2 - Teachers, 3 - Students and 4 - Guardians. </br></br>
				Please contact the admin for the school ID. This is very important else you will end up exposing your users to a different school. If you are the admin the school id is on the home pag, along with the other information about the school. </br></br>
				You do not need to add the enrollment_id unless the user is a Student. </br></br>
				Password and Password_confirmation fields should be equal.
			</div>
		</div>
	</div>
</div>

<%= form_tag import_users_path, multipart: true do %>
  <%= file_field_tag :file %>
  </br>
  <%= submit_tag "Import" %>
<% end %>
