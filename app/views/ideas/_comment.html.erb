<% if @comment.nil? %>
	<% @comment = Comment.new %>
<% end %>

<%= form_for @comment, :html => {:class => 'form-horizontal'} do |f| %>
	<fieldset>
	  <% if @comment.errors.any? %>
		<div id="error_explanation">
		  <h2><%= pluralize(@comment.errors.count, "error") %> prohibited this idea from being saved:</h2>

		  <ul>
		  <% @comment.errors.full_messages.each do |msg| %>
			<li><%= msg %></li>
		  <% end %>
		  </ul>
		</div>
	  <% end %>

	  <%= f.text_area :comment, :cols => "80", :rows => "2" %>
	  
	  <%= button_tag(type: 'Add', class: "btn-small btn-primary") do %>
		<span class="glyphicon glyphicon-comment"></span> 
	  <% end %>

	<%= f.hidden_field :user_id, :value => current_user.id %> 
	<%= f.hidden_field :idea_id, :value => idea_id %>
		
	</fieldset>
<% end %>
